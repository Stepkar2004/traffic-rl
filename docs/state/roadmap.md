# Roadmap

> The stage plan. Authority is the brain note (CLAUDE.md points to it); re-derive here when
> it changes. Chunks, in order.

1. **Metrics & realism constraints locked** (stage 0, current) — before any code.
2. **Simulator.**
3. **Classical baselines** — fixed-time, Webster, actuated, max-pressure. The honesty
   layer: RL that can't beat max-pressure ships as a negative result, not hidden.
4. **RL, single intersection.**
5. **RL, network** — does a green wave emerge?
6. **Writeup.**
