# traffic-rl — traffic-signal scheduling, classical → RL

On start, read `G:\PythonProjects\_for_me\The Brain v2\knowledge\projects\traffic-rl.md`
for the phase plan (the 5-phase reality ladder: world + classical baselines → omniscient
RL → partial observability → humans → beyond the grid). Metrics and realism constraints
are locked at the top of phase 1 BEFORE building. The `project-base` skill in this repo
binds every session.

Public showcase repo, fully-owned code. Baselines are the honesty layer — RL that can't
beat max-pressure gets published as a negative result, not hidden.
